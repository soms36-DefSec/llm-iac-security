# =============================================================================
# requirements.txt
# LLM Agentic Workflow for IaC Vulnerability Detection & Remediation
# Python 3.13 + Windows (x64) compatible
# =============================================================================


# -----------------------------------------------------------------------------
# AWS SDK & Amazon Bedrock
# Used in: llm/bedrock_client.py, storage/s3_handler.py, utils/aws_utils.py,
#          knowledge_base/vector_store.py (OpenSearch Serverless via boto3)
# -----------------------------------------------------------------------------
boto3>=1.38.0
botocore>=1.38.0
awscli>=1.38.0


# -----------------------------------------------------------------------------
# ChromaDB (Vector Store)
# Used in: knowledge_base/vector_store.py, knowledge_base/kb_manager.py
# Free, open-source embedded vector database — runs locally, no API keys
# -----------------------------------------------------------------------------
chromadb>=1.0.0


# -----------------------------------------------------------------------------
# Text Embeddings (Sentence Transformers)
# Used in: knowledge_base/embeddings.py
# Local embedding model (all-MiniLM-L6-v2) — runs on CPU, no API keys
# -----------------------------------------------------------------------------
sentence-transformers>=3.4.0
transformers>=4.51.0
torch>=2.6.0                      # Backend for sentence-transformers (CPU build)


# -----------------------------------------------------------------------------
# YAML / JSON Parsing (CloudFormation Templates)
# Used in: parsers/cloudformation_parser.py, parsers/resource_extractor.py
# -----------------------------------------------------------------------------
pypdf>=5.1.0                      # PDF extraction for knowledge base documents
PyYAML>=6.0.2
ruamel.yaml>=0.18.6               # Round-trip YAML parsing (preserves comments)
jsonschema>=4.23.0                # JSON schema validation for CF template inputs


# -----------------------------------------------------------------------------
# LLM Prompt Engineering & Response Parsing
# Used in: llm/prompt_builder.py, llm/response_parser.py,
#          llm/prompt_templates.py
# -----------------------------------------------------------------------------
jinja2>=3.1.5                     # Templating engine for dynamic prompt construction
tiktoken>=0.9.0                   # Token counting for prompt length management
pydantic>=2.11.0                  # Structured output validation & data models
pydantic-settings>=2.9.0          # Settings management via .env files


# -----------------------------------------------------------------------------
# Markdown Report Generation
# Used in: reporting/report_builder.py, reporting/markdown_formatter.py
# -----------------------------------------------------------------------------
markdown>=3.7
mdformat>=0.7.22                  # Programmatic Markdown formatting/normalization


# -----------------------------------------------------------------------------
# HTTP Requests (Knowledge Base Updater / AWS Docs Fetching)
# Used in: knowledge_base/updater.py
# -----------------------------------------------------------------------------
requests>=2.32.0
httpx>=0.28.0                     # Async-capable HTTP client (used for Bedrock streaming)
aiohttp>=3.11.0                   # Async HTTP for parallel document fetching


# -----------------------------------------------------------------------------
# Data Processing & Utilities
# Used in: parsers/resource_extractor.py, scripts/evaluate_results.py
# -----------------------------------------------------------------------------
numpy>=2.2.6                      # Numerical ops for embedding vectors
pandas>=2.3.0                     # Tabular result analysis (evaluation metrics)
scikit-learn>=1.6.0               # Precision / recall / F1 computation (Python 3.13 wheels available)
tqdm>=4.67.0                      # Progress bars for batch template scans


# -----------------------------------------------------------------------------
# Local Caching
# Used in: storage/local_cache.py
# -----------------------------------------------------------------------------
diskcache>=5.6.3                  # Disk-backed LRU cache for embeddings/responses
joblib>=1.4.2                     # Efficient serialization of numpy arrays


# -----------------------------------------------------------------------------
# Input Validation & Schema
# Used in: utils/validation.py
# -----------------------------------------------------------------------------
cerberus>=1.3.5                   # Lightweight schema validator for CF resource dicts
python-dotenv>=1.1.0              # Load AWS credentials & config from .env files


# -----------------------------------------------------------------------------
# Logging & Observability
# Used in: config/logging_config.py, all agents
# -----------------------------------------------------------------------------
structlog>=24.4.0                 # Structured JSON logging
rich>=13.9.0                      # Pretty console output & logging


# -----------------------------------------------------------------------------
# CLI Entry Point
# Used in: scripts/run_scan.py
# -----------------------------------------------------------------------------
click>=8.1.8                      # CLI argument parsing for run_scan.py
colorama>=0.4.6                   # Cross-platform terminal colour support


# -----------------------------------------------------------------------------
# CI/CD Integration
# Used in: cicd/pipeline_integration.py
# -----------------------------------------------------------------------------
python-gitlab>=4.10.0             # GitLab API client (for MR annotations)
PyGithub>=2.5.0                   # GitHub API client (for PR check annotations)


# -----------------------------------------------------------------------------
# Testing
# Used in: tests/ (unit + integration)
# -----------------------------------------------------------------------------
pytest>=8.3.0
pytest-cov>=5.0.0                 # Coverage reporting
pytest-mock>=3.14.0               # Mocking AWS / Bedrock calls
pytest-asyncio>=0.25.0            # Async test support
moto[s3,opensearch]>=5.0.24       # AWS service mocking (S3, OpenSearch)
responses>=0.25.3                 # HTTP request mocking for updater tests
freezegun>=1.5.1                  # Freeze time for deterministic test runs


# -----------------------------------------------------------------------------
# Code Quality (dev / CI)
# -----------------------------------------------------------------------------
black>=24.10.0                    # Code formatter
isort>=5.13.2                     # Import sorter
flake8>=7.2.0                     # Linter
mypy>=1.13.0                      # Static type checker
pre-commit>=4.0.0                 # Git pre-commit hook runner
